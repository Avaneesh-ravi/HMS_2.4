{
  "name": "hospital-feedback-system",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix frontend_source run build",
    "vercel-build": "npm --prefix frontend_source install && npm --prefix frontend_source run build"
  },
  "dependencies": {
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "pdf-lib": "^1.17.1",
    "puppeteer-core": "^25.11.0"
  }
}
